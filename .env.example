# Copy this file to `.env` and fill in your values. `.env` is gitignored and is the
# ONLY place secrets are stored — never put keys in the scripts or the workflow.

ANTHROPIC_API_KEY=
GMAIL_ADDRESS=you@gmail.com
GMAIL_APP_PASSWORD=
RECIPIENT=you@gmail.com
NEWSLETTER_MODEL=claude-sonnet-5
